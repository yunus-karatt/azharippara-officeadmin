import React from "react";

const Loading = () => {
  return (
    <div className="h-screen w-screen flex items-center justify-center bg-black/30 backdrop-blur-[2px] fixed top-0 left-0 z-50">
      Loading...
    </div>
  );
};

export default Loading;
